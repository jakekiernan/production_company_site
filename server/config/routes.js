var controllers = require('./../controllers/controller.js');

module.exports=(app)=>{
  // app.post('/api/login', controllers.getUser);
  // app.post('/api/new', controllers.newQuestion);
  // app.get('/api/get_questions', controllers.getQuestions);
  // app.get('/api/question/:id', controllers.questionInfo);
  // app.post('/api/newanswer/:id', controllers.newAnswer);
  // app.get('/logout', controllers.logout);
  // app.get('/api/likeanswer/:id', controllers.likeAnswer);
  // app.get('/api/current', controllers.current);

}